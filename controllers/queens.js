import { Queen } from "../models/queen.js"

function newQueen(req, res) {
  res.render('queens/new', {
      title: "New Queen",
  })
  .catch(err => {
    console.log(err)
    res.redirect("/")
  })
}

export {
  newQueen as new,
}